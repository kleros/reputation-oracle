---
phase: quick
plan: 260423-2ev
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/src/chain.ts
autonomous: true
requirements:
  - IN-01
must_haves:
  truths:
    - "Dead fallback `?? 30_000` is removed from chain.ts:228"
    - "TypeScript compiles without errors"
    - "Vitest test suite passes"
  artifacts:
    - path: bot/src/chain.ts
      provides: "uploadEvidenceToIPFS call without unreachable fallback"
  key_links: []
---

<objective>
Remove the dead `?? 30_000` null-coalescing fallback on `config.PINATA_TIMEOUT_MS` at
bot/src/chain.ts:228.

Purpose: The zod schema for PINATA_TIMEOUT_MS is `.optional().default(30_000)`, which means
the field is typed `number` (never `undefined`). The `?? 30_000` fallback is unreachable and
misleads readers into thinking the field can be absent. Removing it aligns the code with the
type system and eliminates dead code flagged as IN-01 in 06-REVIEW.md.

Output: bot/src/chain.ts with the one-character fix applied.
</objective>

<execution_context>
@/Users/jaybuidl/project/kleros/reputation-oracle/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/jaybuidl/project/kleros/reputation-oracle/bot/src/chain.ts
@/Users/jaybuidl/project/kleros/reputation-oracle/bot/src/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead fallback on PINATA_TIMEOUT_MS</name>
  <files>bot/src/chain.ts</files>
  <action>
On line 228 of bot/src/chain.ts, change:

    config.PINATA_TIMEOUT_MS ?? 30_000,

to:

    config.PINATA_TIMEOUT_MS,

No other changes. The zod schema already guarantees a numeric value via `.default(30_000)`,
so the `?? 30_000` fallback is unreachable dead code (IN-01 from 06-REVIEW.md).

After editing, run:
  1. `pnpm exec tsc --noEmit` from the bot/ directory — must produce zero errors
  2. `pnpm exec vitest run` from the bot/ directory — all tests must pass
  </action>
  <verify>
    <automated>cd /Users/jaybuidl/project/kleros/reputation-oracle/bot && pnpm exec tsc --noEmit && pnpm exec vitest run</automated>
  </verify>
  <done>Line 228 reads `config.PINATA_TIMEOUT_MS,` with no fallback; tsc and vitest both pass cleanly.</done>
</task>

</tasks>

<verification>
grep -n "PINATA_TIMEOUT_MS" /Users/jaybuidl/project/kleros/reputation-oracle/bot/src/chain.ts
# Must show `config.PINATA_TIMEOUT_MS,` — no `?? 30_000` on that line.
</verification>

<success_criteria>
- bot/src/chain.ts line 228: `config.PINATA_TIMEOUT_MS,` (no nullish coalescing)
- `tsc --noEmit` exits 0
- `vitest run` exits 0
</success_criteria>

<output>
No SUMMARY.md required for quick fixes. The fix is self-contained and tracked via the
06-REVIEW.md IN-01 finding.
</output>
