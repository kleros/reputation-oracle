---
phase: quick-260424-23x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - deploy/RUNBOOK.md
  - deploy/bootstrap.sh
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/research/STACK.md
  - .planning/research/FEATURES.md
autonomous: true
requirements: [PKG-01]

must_haves:
  truths:
    - "No active/authoritative doc mentions Ubuntu 24.04"
    - "All factual claims tied to Ubuntu version are corrected (Node default, systemd version)"
    - "Archived phase artifacts (Category B) are untouched"
  artifacts:
    - path: "README.md"
      provides: "Corrected OS reference in Deployment section"
      contains: "Ubuntu 22.04"
    - path: "deploy/RUNBOOK.md"
      provides: "Corrected OS in Target + Prerequisites"
      contains: "Ubuntu 22.04"
    - path: "deploy/bootstrap.sh"
      provides: "Corrected OS in comment header"
      contains: "Ubuntu 22.04"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected PKG-01 acceptance criterion"
      contains: "Ubuntu 22.04"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 7 Goal"
      contains: "Ubuntu 22.04"
    - path: ".planning/research/STACK.md"
      provides: "All 8 occurrences corrected; secondary factual claims fixed"
    - path: ".planning/research/FEATURES.md"
      provides: "Corrected systemd version claim"
  key_links: []
---

<objective>
Correct VPS OS references from Ubuntu 24.04 to Ubuntu 22.04 across all active/authoritative documents.

Purpose: Factual accuracy — the live VPS runs Ubuntu 22.04, not 24.04. Active docs are the operator's reference during incidents; incorrect OS version could cause confusion during troubleshooting or re-provisioning.
Output: 7 files updated; archived phase/quick artifacts (Category B) untouched.
</objective>

<execution_context>
@/Users/jaybuidl/project/kleros/reputation-oracle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaybuidl/project/kleros/reputation-oracle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct Ubuntu version in all active/authoritative docs</name>
  <files>
    README.md,
    deploy/RUNBOOK.md,
    deploy/bootstrap.sh,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/research/STACK.md,
    .planning/research/FEATURES.md
  </files>
  <action>
Apply the following exact substitutions. Do NOT touch any file under `.planning/phases/` or `.planning/quick/` (Category B — archived/read-only).

**README.md line 93:**
- Before: `The bot runs on Ubuntu 24.04 via a systemd timer...`
- After:   `The bot runs on Ubuntu 22.04 via a systemd timer...`

**deploy/RUNBOOK.md line 3:**
- Before: `**Target:** Ubuntu 24.04 VPS, Node 22 via NodeSource...`
- After:   `**Target:** Ubuntu 22.04 VPS, Node 22 via NodeSource...`

**deploy/RUNBOOK.md line 25:**
- Before: `**Prerequisites:** Ubuntu 24.04 VPS, SSH access...`
- After:   `**Prerequisites:** Ubuntu 22.04 VPS, SSH access...`

**deploy/bootstrap.sh line 5 (comment only):**
- Before: `# Assumptions: fresh Ubuntu 24.04 VPS...`
- After:   `# Assumptions: fresh Ubuntu 22.04 VPS...`

**.planning/REQUIREMENTS.md line 12 (PKG-01):**
- Before: `Bot runs on fresh Ubuntu 24.04 VPS via a single idempotent \`bootstrap.sh\``
- After:   `Bot runs on fresh Ubuntu 22.04 VPS via a single idempotent \`bootstrap.sh\``

**.planning/ROADMAP.md line 52 (Phase 7 Goal):**
- Before: `Bot runs reliably on a fresh Ubuntu 24.04 VPS via a single idempotent bootstrap script...`
- After:   `Bot runs reliably on a fresh Ubuntu 22.04 VPS via a single idempotent bootstrap script...`

**.planning/research/STACK.md — multiple occurrences:**
- Section heading (line 15): `## 1. Packaging — Node 22 on Ubuntu 24.04` → `## 1. Packaging — Node 22 on Ubuntu 22.04`
- Line 19: `Ubuntu 24.04 ships Node.js 18 in its default apt.` → `Ubuntu 22.04 ships Node.js 12 in its default apt.`
  (Ubuntu 22.04 LTS ships Node.js 12.x from distro apt — not 18. The conclusion "Do not use the distro package" is unchanged.)
- Line 64 comment: `# Sandboxing (systemd 255+, Ubuntu 24.04 ships 255.4)` → `# Sandboxing (systemd 245+, Ubuntu 22.04 ships 249)`
  (Ubuntu 22.04 ships systemd 249; all directives still require 245+ so the conclusion "All directives available" is unchanged.)
- Line 115: `**systemd version context:** Ubuntu 24.04 ships systemd 255.4.` → `**systemd version context:** Ubuntu 22.04 ships systemd 249.`
  (Remaining sentence "All directives above are available in systemd 245+" stays — still true for 249.)
- Line 156: `Ubuntu 24.04 enables persistent journald storage by default` → `Ubuntu 22.04 enables persistent journald storage by default`
- Line 404: `| systemd 255.4 (Ubuntu 24.04) | all directives require systemd 245+ | All directives available |`
           → `| systemd 249 (Ubuntu 22.04) | all directives require systemd 245+ | All directives available |`
- Line 410: `Node 22 on Ubuntu 24.04; NodeSource apt method` → `Node 22 on Ubuntu 22.04; NodeSource apt method`
- Line 421: `journald log rotation Ubuntu 24.04` → `journald log rotation Ubuntu 22.04`

**.planning/research/FEATURES.md line 23:**
- Before: `All available on Ubuntu 24.04 (systemd 255.4)`
- After:   `All available on Ubuntu 22.04 (systemd 249)`
  (Argument intact: 249 > 245, so all directives remain available.)
  </action>
  <verify>
    <automated>grep -rn "Ubuntu 24.04" README.md deploy/RUNBOOK.md deploy/bootstrap.sh .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/research/STACK.md .planning/research/FEATURES.md && echo "FAIL: 24.04 still present" || echo "PASS: no 24.04 in active docs"</automated>
  </verify>
  <done>
    - Zero occurrences of "Ubuntu 24.04" in the 7 active/authoritative files
    - Secondary factual claims corrected: Node default = 12 (22.04), systemd = 249 (22.04)
    - No file under .planning/phases/ or .planning/quick/ was modified
  </done>
</task>

</tasks>

<verification>
After task completes:

```bash
# Confirm no 24.04 in active docs
grep -rn "Ubuntu 24.04" README.md deploy/RUNBOOK.md deploy/bootstrap.sh .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/research/STACK.md .planning/research/FEATURES.md

# Confirm 22.04 is present in each file
grep -n "Ubuntu 22.04" README.md deploy/RUNBOOK.md deploy/bootstrap.sh .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/research/STACK.md .planning/research/FEATURES.md

# Confirm archived artifacts untouched
grep -rn "Ubuntu 24.04" .planning/phases/ .planning/quick/ | grep -v "260424-23x" | wc -l
# (should be nonzero — those archived files still say 24.04 and that is correct)
```
</verification>

<success_criteria>
- All 7 active/authoritative files say "Ubuntu 22.04"
- Secondary factual claims (Node default = 12, systemd = 249) corrected in STACK.md + FEATURES.md
- Logical conclusions unchanged: "Do not use distro package", "All directives available"
- Archived phase/quick artifacts (Category B) not modified
</success_criteria>

<output>
After completion, create `.planning/quick/260424-23x-correct-vps-os-to-ubuntu-22-04-in-docs/260424-23x-SUMMARY.md` with:
- Files modified (list)
- Substitutions made
- Commit hash
</output>
