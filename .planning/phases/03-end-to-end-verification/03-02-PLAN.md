---
phase: 03-end-to-end-verification
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .planning/phases/03-end-to-end-verification/03-VERIFICATION.md
autonomous: false
requirements: [VER-01, VER-02, VER-03, VER-04]

user_setup:
  - service: sepolia-router
    why: "Router must be deployed to Sepolia before bot can execute"
    env_vars:
      - name: DEPLOYER_PRIVATE_KEY
        source: "User's deployer wallet private key"
      - name: BOT_PRIVATE_KEY
        source: "User's bot wallet private key (needs Sepolia ETH)"
      - name: SEPOLIA_RPC_URL
        source: "Alchemy/Infura Sepolia RPC URL"
      - name: BOT_ADDRESS
        source: "Address derived from BOT_PRIVATE_KEY"
      - name: KLEROS_AGENT_URI
        source: "IPFS or HTTPS URI for Kleros agent metadata"
    dashboard_config:
      - task: "Deploy Router via Deploy.s.sol --broadcast"
        location: "Terminal — user broadcasts the Foundry deploy script"

must_haves:
  truths:
    - "Router is deployed on Sepolia and the proxy address is known"
    - "Bot runs against real subgraph and submits real transactions to the deployed Router"
    - "After bot run, getSummary returns count=1, value=95 for touched agents (VER-01)"
    - "Second bot dry-run shows 0 actions, proving idempotency (D-02)"
    - "Fork tests already pass for Scenarios 2/3, proving VER-02 and VER-03"
    - "Tag filtering via getSummary with 'verified'/'removed' returns correct results (VER-04)"
  artifacts:
    - path: ".planning/phases/03-end-to-end-verification/03-VERIFICATION.md"
      provides: "Complete verification report with console output evidence"
      contains: "VER-01"
      min_lines: 30
  key_links:
    - from: "bot/src/index.ts"
      to: "contracts (Router proxy)"
      via: "ROUTER_ADDRESS env var pointing to deployed proxy"
      pattern: "ROUTER_ADDRESS"
    - from: "contracts/script/Verify.s.sol"
      to: "IReputationRegistry.getSummary"
      via: "on-chain call to REPUTATION_REGISTRY"
      pattern: "getSummary"
---

<objective>
Execute the full E2E verification pipeline on Sepolia: deploy Router (user prerequisite), run bot against real subgraph, verify results via Verify.s.sol, prove idempotency, and capture all evidence in 03-VERIFICATION.md.

Purpose: Per D-01 and D-02, this plan proves the complete pipeline works on Sepolia and documents the evidence. This is the final proof that the Kleros Reputation Oracle produces correct ERC-8004 reputation.
Output: .planning/phases/03-end-to-end-verification/03-VERIFICATION.md
</objective>

<execution_context>
@/Users/jaybuidl/project/kleros/reputation-oracle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaybuidl/project/kleros/reputation-oracle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-end-to-end-verification/03-CONTEXT.md
@.planning/phases/03-end-to-end-verification/03-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy Router to Sepolia (user prerequisite)</name>
  <files>contracts/script/Deploy.s.sol</files>
  <read_first>
    - contracts/script/Deploy.s.sol (deploy script the user will run)
    - .planning/phases/03-end-to-end-verification/03-CONTEXT.md (D-03, D-04 — deployment is manual)
  </read_first>
  <action>
Per D-03, Router deployment is a manual prerequisite. The user must:

1. Ensure deployer wallet has Sepolia ETH
2. Ensure bot wallet has Sepolia ETH for gas
3. Set env vars: DEPLOYER_PRIVATE_KEY, BOT_ADDRESS, KLEROS_AGENT_URI, SEPOLIA_RPC_URL
4. Run Deploy.s.sol with --broadcast:
   ```bash
   cd contracts
   forge script script/Deploy.s.sol --rpc-url $SEPOLIA_RPC_URL --private-key $DEPLOYER_PRIVATE_KEY --broadcast
   ```
5. Save the ROUTER_PROXY_ADDRESS from the output

After deployment, provide:
- ROUTER_PROXY_ADDRESS (the proxy address from step 5)
- Confirmation that bot wallet is authorized
  </action>
  <verify>User provides the deployed ROUTER_PROXY_ADDRESS and confirms bot authorization</verify>
  <acceptance_criteria>
    - User has provided a valid Ethereum address for ROUTER_PROXY_ADDRESS (0x... format, 42 chars)
    - User confirms bot is authorized on the Router
  </acceptance_criteria>
  <done>Router deployed on Sepolia, proxy address known, bot authorized</done>
</task>

<task type="auto">
  <name>Task 2: Run bot against Sepolia and execute Verify.s.sol</name>
  <files>.planning/phases/03-end-to-end-verification/03-VERIFICATION.md</files>
  <read_first>
    - contracts/script/Verify.s.sol (verification script to run)
    - bot/src/index.ts (bot entry point)
    - bot/package.json (npm scripts)
    - .planning/phases/03-end-to-end-verification/03-CONTEXT.md (D-01, D-02, D-05, D-07, D-08, D-09, D-10)
  </read_first>
  <action>
Execute the full E2E verification sequence. All commands run from project root. The ROUTER_PROXY_ADDRESS will be provided by the user from Task 1.

**Step 1 — Bot live run (Scenario 1 E2E):**
Per D-01 and D-05, run the bot against real Sepolia subgraph items:
```bash
cd bot
# User must have .env with: CHAIN_ID=11155111, RPC_URL, BOT_PRIVATE_KEY, SUBGRAPH_URL, ROUTER_ADDRESS={from Task 1}, PGTCR_ADDRESS=0x3162df9669affa8b6b6ff2147afa052249f00447
npm run start
```
Capture the console output (number of actions executed, transaction hashes).

**Step 2 — Idempotency proof (D-02):**
Run bot again in dry-run mode. Second run MUST show 0 actions:
```bash
npm run start:dry-run
```
Capture the output. Expected: "Computed 0 actions" and "Dry run complete: 0 actions would be executed"

**Step 3 — Verify.s.sol (VER-01, VER-04):**
Run the verification script against the deployed Router:
```bash
cd ../contracts
ROUTER_PROXY_ADDRESS={from Task 1} AGENT_IDS="610,1142,1143,1440" forge script script/Verify.s.sol --rpc-url $SEPOLIA_RPC_URL -vvvv
```
Capture the output showing all assertions passed.

**Step 4 — Fork tests confirmation (VER-02, VER-03):**
Run the existing fork tests to confirm Scenarios 2/3 still pass:
```bash
forge test --fork-url $SEPOLIA_RPC_URL -vv
```
Capture relevant output showing all tests pass.

**Step 5 — Write 03-VERIFICATION.md (D-08):**
Create `.planning/phases/03-end-to-end-verification/03-VERIFICATION.md` with:

```markdown
# Phase 3: End-to-End Verification Report

**Date:** {today}
**Network:** Sepolia (chainId 11155111)
**Router Proxy:** {ROUTER_PROXY_ADDRESS from Task 1}

## VER-01: Scenario 1 — Positive Feedback (E2E Proven)

**Status:** PASS

After bot run, getSummary(agentId, [router], "", "") returns count=1, value=95 for all touched agents.

### Bot Run Output
{paste Step 1 output}

### Idempotency Proof (D-02)
{paste Step 2 output — 0 actions on second run}

### Verify.s.sol Output
{paste Step 3 output}

## VER-02: Scenario 2 — Negative Feedback (Fork Test Proven)

**Status:** PASS (fork tests) | Pending live E2E (no disputed items on PGTCR list yet)

Per D-10, proven via `test_submitNegativeFeedback_revokesPositiveThenSubmitsNegative` in KlerosReputationRouter.t.sol.
Asserts: count=1, value=-95 after revoke-then-negative.

## VER-03: Scenario 3 — Revoke Only (Fork Test Proven)

**Status:** PASS (fork tests) | Pending live E2E (no voluntary withdrawals on PGTCR list yet)

Per D-10, proven via `test_revokeOnly_removesPositiveFeedback` in KlerosReputationRouter.t.sol.
Asserts: count=0 after revoke.

## VER-04: Tag Filtering (E2E Proven)

**Status:** PASS

Verify.s.sol confirms:
- getSummary with tag1="verified" returns count=1 for Scenario 1 agents
- getSummary with tag1="removed" returns count=0 for Scenario 1 agents

### Fork Test Output
{paste Step 4 output}

## Summary

| Requirement | Status | Evidence |
|-------------|--------|----------|
| VER-01 | PASS (E2E) | Bot run + Verify.s.sol |
| VER-02 | PASS (fork) | KlerosReputationRouter.t.sol |
| VER-03 | PASS (fork) | KlerosReputationRouter.t.sol |
| VER-04 | PASS (E2E) | Verify.s.sol tag filtering |
```

Adapt the template above based on actual command outputs. If any step fails, document the failure and the diagnosis.
  </action>
  <verify>
    <automated>test -f /Users/jaybuidl/project/kleros/reputation-oracle/.planning/phases/03-end-to-end-verification/03-VERIFICATION.md && grep -c "VER-0" /Users/jaybuidl/project/kleros/reputation-oracle/.planning/phases/03-end-to-end-verification/03-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/03-end-to-end-verification/03-VERIFICATION.md
    - Contains "VER-01" with status PASS
    - Contains "VER-02" with status referencing fork tests
    - Contains "VER-03" with status referencing fork tests
    - Contains "VER-04" with status PASS
    - Contains "Idempotency" section showing 0 actions on second run
    - Contains Router proxy address
    - Contains bot run output (transaction evidence)
    - Contains Verify.s.sol output (assertion evidence)
  </acceptance_criteria>
  <done>03-VERIFICATION.md created with complete evidence for all 4 VER requirements. VER-01 and VER-04 proven via live E2E. VER-02 and VER-03 proven via fork tests per D-10.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review verification results</name>
  <files>.planning/phases/03-end-to-end-verification/03-VERIFICATION.md</files>
  <action>
Human reviews the 03-VERIFICATION.md report and optionally spot-checks on-chain state via cast or 8004scan.
  </action>
  <what-built>Complete E2E verification of the Kleros Reputation Oracle on Sepolia. Bot submitted real transactions, Verify.s.sol confirmed correct getSummary values, idempotency proven via second dry-run.</what-built>
  <how-to-verify>
    1. Read .planning/phases/03-end-to-end-verification/03-VERIFICATION.md
    2. Confirm VER-01: getSummary returns count=1, value=95 for touched agents
    3. Confirm VER-04: tag filtering works (verified=1, removed=0)
    4. Confirm idempotency: second dry-run shows 0 actions
    5. Optionally verify on 8004scan or directly via cast:
       ```bash
       cast call 0x8004B663056A597Dffe9eCcC1965A193B7388713 "getSummary(uint256,address[],string,string)" 610 "[ROUTER_PROXY_ADDRESS]" "" "" --rpc-url $SEPOLIA_RPC_URL
       ```
  </how-to-verify>
  <verify>User reviews 03-VERIFICATION.md and confirms all VER requirements are satisfied</verify>
  <done>User approves verification results, Phase 3 complete</done>
  <resume-signal>Type "approved" to complete Phase 3, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Bot run completes with exit code 0 and submits transactions
- Second dry-run shows 0 actions (idempotency)
- Verify.s.sol exits with no assertion failures
- Fork tests pass (scenarios 2/3)
- 03-VERIFICATION.md captures all evidence
</verification>

<success_criteria>
- Bot successfully executed against real Sepolia subgraph and Router
- getSummary returns count=1, value=95 for all touched agents (VER-01)
- Tag filtering works correctly via Verify.s.sol (VER-04)
- Idempotency proven: second run shows 0 actions (D-02)
- Fork tests confirm Scenarios 2/3 (VER-02, VER-03)
- 03-VERIFICATION.md documents all results with captured console output
</success_criteria>

<output>
After completion, create `.planning/phases/03-end-to-end-verification/03-02-SUMMARY.md`
</output>
