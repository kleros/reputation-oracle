---
phase: 08-observability
plan: "06"
type: execute
wave: 3
depends_on:
  - "08-01"
files_modified:
  - deploy/bootstrap.sh
  - deploy/RUNBOOK.md
autonomous: true
requirements:
  - OBS-07
  - OBS-08

must_haves:
  truths:
    - "bootstrap.sh heredoc has BETTERSTACK_HEARTBEAT_URL (not BETTERSTACK_HEARTBEAT_TOKEN typo)"
    - "bootstrap.sh heredoc has HEARTBEAT_TIMEOUT_MS=10000 placeholder"
    - "RUNBOOK.md §9 Betterstack Setup documents source token creation and alert rule ClickHouse SQL"
    - "RUNBOOK.md §10 Burn-in Gate Procedure documents 7-day Sepolia gate checklist before Phase 9"
    - "RUNBOOK.md §2 Secret Fill Procedure updated to reference correct Phase 8 key names"
    - "Grace period documented as 600 seconds (10 minutes) matching D-04"
    - "itemsFetched===0 alert threshold documented as 5 consecutive runs (D-24)"
  artifacts:
    - path: "deploy/bootstrap.sh"
      provides: "Corrected Phase 8 env var placeholders in sepolia.env heredoc"
      contains: "BETTERSTACK_HEARTBEAT_URL"
    - path: "deploy/RUNBOOK.md"
      provides: "§9 Betterstack Setup + §10 Burn-in Gate Procedure"
      contains: "Burn-in Gate"
  key_links:
    - from: "deploy/RUNBOOK.md §9"
      to: "Betterstack dashboard UI"
      via: "operator reads RUNBOOK to configure Betterstack source token and heartbeat monitor"
      pattern: "BETTERSTACK_SOURCE_TOKEN"
    - from: "deploy/RUNBOOK.md §10"
      to: "Phase 9 Mainnet Cutover gate"
      via: "7 consecutive successful heartbeats required before enabling Mainnet timer"
      pattern: "burn-in"
---

<objective>
Fix the bootstrap.sh heredoc typo and add HEARTBEAT_TIMEOUT_MS placeholder. Append §9 Betterstack Setup and §10 Burn-in Gate Procedure to deploy/RUNBOOK.md. Update §2 to reference correct Phase 8 key names.

Purpose: OBS-07 (dashboard documented) and OBS-08 (alert rule documented). Wave 3 because it documents the full Phase 8 feature set; depends on plan 01 for the exact env var names.
Output: bootstrap.sh with correct placeholders; RUNBOOK.md with complete Betterstack onboarding and burn-in gate documentation.
</objective>

<execution_context>
@/Users/jaybuidl/project/kleros/reputation-oracle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaybuidl/project/kleros/reputation-oracle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-observability/08-CONTEXT.md
@.planning/phases/08-observability/08-RESEARCH.md

<interfaces>
<!-- bootstrap.sh heredoc current state (lines 103-126) -->
From deploy/bootstrap.sh (lines 122-125, the Phase 8 stub):
```bash
# ── Phase 8: Observability (fill after Betterstack setup) ───────────────────
# BETTERSTACK_SOURCE_TOKEN=
# BETTERSTACK_HEARTBEAT_TOKEN=      ← TYPO: must be BETTERSTACK_HEARTBEAT_URL
                                     ← MISSING: HEARTBEAT_TIMEOUT_MS=10000
ENVTEMPLATE
```
The heredoc is inside `if [ ! -f /etc/reputation-oracle/sepolia.env ]` block (never-clobber per Phase 7 D-19).

<!-- deploy/RUNBOOK.md current section structure -->
Current sections: 1-8 (see Table of Contents).
- §2 Secret Fill Procedure references "BETTERSTACK_HEARTBEAT_TOKEN" (wrong name — same typo)
- New sections to append: §9 Betterstack Setup, §10 Burn-in Gate Procedure
- Table of Contents must be updated to include §9 and §10

<!-- D-04: grace period = 600 seconds = 10 minutes -->
<!-- D-24: alert threshold = 5 consecutive runs with itemsFetched === 0 -->
<!-- D-03: burn-in is 7 consecutive successful heartbeats before Phase 9 -->
<!-- RESEARCH.md §Betterstack APIs: -->
//   ClickHouse SQL for itemsFetched alert:
//   SELECT count()
//   FROM remote(t<source_id>_your_source_logs)
//   WHERE JSONExtract(raw, 'summary.itemsFetched', 'Nullable(Int64)') = 0
//     AND {{time}}
//   Set threshold ≥ 5, confirmation period 25 min
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix bootstrap.sh heredoc Phase 8 placeholders</name>
  <files>deploy/bootstrap.sh</files>

  <read_first>
    - deploy/bootstrap.sh — lines 95-130 (full heredoc region: the ENVTEMPLATE block in step 7)
    - .planning/phases/08-observability/08-CONTEXT.md — D-27 (exact env var names), D-28 (placeholder format)
    - .planning/phases/08-observability/08-PATTERNS.md — §bootstrap.sh section (exact diff)
  </read_first>

  <action>
In `deploy/bootstrap.sh`, find the Phase 8 block inside the ENVTEMPLATE heredoc (lines ~122-125):

```bash
# ── Phase 8: Observability (fill after Betterstack setup) ───────────────────
# BETTERSTACK_SOURCE_TOKEN=
# BETTERSTACK_HEARTBEAT_TOKEN=
```

Replace with:
```bash
# ── Phase 8: Observability (fill after Betterstack setup) ───────────────────
# BETTERSTACK_SOURCE_TOKEN=
# BETTERSTACK_HEARTBEAT_URL=
# HEARTBEAT_TIMEOUT_MS=10000
```

Changes:
1. `BETTERSTACK_HEARTBEAT_TOKEN=` → `BETTERSTACK_HEARTBEAT_URL=` (fix typo — correct env var name per D-27)
2. Add `# HEARTBEAT_TIMEOUT_MS=10000` on the next line (new placeholder with default per D-27)

Keep all other heredoc content unchanged. The block is inside `if [ ! -f /etc/reputation-oracle/sepolia.env ]` — idempotency preserved (already-deployed VPSs keep their existing file).

Verify the fix:
```bash
grep -n "BETTERSTACK_HEARTBEAT" deploy/bootstrap.sh
```
Expected: returns `BETTERSTACK_HEARTBEAT_URL=` (not BETTERSTACK_HEARTBEAT_TOKEN).
```bash
grep -n "HEARTBEAT_TIMEOUT_MS" deploy/bootstrap.sh
```
Expected: returns the new placeholder line.
  </action>

  <verify>
    <automated>grep -n "BETTERSTACK_HEARTBEAT_URL\|HEARTBEAT_TIMEOUT_MS\|BETTERSTACK_HEARTBEAT_TOKEN" /Users/jaybuidl/project/kleros/reputation-oracle/deploy/bootstrap.sh</automated>
  </verify>

  <acceptance_criteria>
    - `grep "BETTERSTACK_HEARTBEAT_TOKEN" deploy/bootstrap.sh` — returns EMPTY (typo fixed)
    - `grep "BETTERSTACK_HEARTBEAT_URL" deploy/bootstrap.sh` — returns the placeholder line
    - `grep "HEARTBEAT_TIMEOUT_MS" deploy/bootstrap.sh` — returns the placeholder line
    - `bash -n deploy/bootstrap.sh` — exits 0 (bash syntax check passes)
  </acceptance_criteria>

  <done>bootstrap.sh heredoc has BETTERSTACK_HEARTBEAT_URL and HEARTBEAT_TIMEOUT_MS=10000; BETTERSTACK_HEARTBEAT_TOKEN typo gone; bash syntax valid.</done>
</task>

<task type="auto">
  <name>Task 2: Append §9 Betterstack Setup and §10 Burn-in Gate to RUNBOOK.md; fix §2 typo</name>
  <files>deploy/RUNBOOK.md</files>

  <read_first>
    - deploy/RUNBOOK.md — FULL FILE (current 279 lines; note exact section format, table format, code block style)
    - .planning/phases/08-observability/08-RESEARCH.md — §Betterstack APIs (Telemetry ClickHouse SQL, Uptime ping URL, grace period in seconds)
    - .planning/phases/08-observability/08-CONTEXT.md — D-03 (burn-in manual), D-04 (grace 600s/10min), D-24 (alert threshold 5 runs), D-26 (dashboard docs location)
    - .planning/phases/08-observability/08-PATTERNS.md — §RUNBOOK.md section (new sections to append)
  </read_first>

  <action>
Make three changes to `deploy/RUNBOOK.md`:

**Change 1: Fix typo in §2 Secret Fill Procedure (Phase 8 keys reference)**

Find the line in §2:
```
| `BETTERSTACK_SOURCE_TOKEN`, `BETTERSTACK_HEARTBEAT_TOKEN`) — leave commented ...
```

Replace with:
```
| Phase 8 keys (`BETTERSTACK_SOURCE_TOKEN`, `BETTERSTACK_HEARTBEAT_URL`, `HEARTBEAT_TIMEOUT_MS`) — leave commented out until Phase 8 Observability is set up (Betterstack account required). |
```

Also fix the §3 First-Run Verification acceptance table — the `itemsFetched > 0` row is already correct (RUNBOOK.md was written anticipating the rename). Verify no `summary.items` references remain.

**Change 2: Update Table of Contents**

Add to the Table of Contents (after §8):
```
9. [Betterstack Setup](#9-betterstack-setup)
10. [Burn-in Gate Procedure](#10-burn-in-gate-procedure)
```

**Change 3: Append §9 and §10 at end of file**

Append the following content after the current end of file (after §8 Troubleshooting):

```markdown

---

## 9. Betterstack Setup

**Prerequisites:** Betterstack account (free tier sufficient for v1.2). Betterstack tokens must be filled in `/etc/reputation-oracle/sepolia.env` before the bot can forward logs or send heartbeats.

### 9.1 Telemetry Source (Log Forwarding)

1. Log in to [https://logs.betterstack.com](https://logs.betterstack.com)
2. Go to **Sources** → **Connect source** → Select **Node.js** (uses pino transport)
3. Copy the **Source token** shown on the configuration page
4. On the VPS: `sudo -u oracle nano /etc/reputation-oracle/sepolia.env`
   - Set `BETTERSTACK_SOURCE_TOKEN=<paste token here>`
   - Uncomment the line (remove leading `#`)
5. Restart the timer to apply: `sudo systemctl restart reputation-oracle@sepolia.timer`
6. Wait for the next scheduled run (within 5 minutes), then verify in Betterstack Logs that entries appear with `runId` and `chainId` fields.

**Filter by run:** In Betterstack Telemetry search bar, enter the `runId` UUID from a specific run (e.g. `a1b2c3d4-e5f6-7890-abcd-ef1234567890`).

**itemsFetched === 0 alert (OBS-08):**

Create an alert in Betterstack Telemetry to detect silent list-misconfiguration (5 consecutive empty runs):

1. In Betterstack Logs → **Alerts** → **New alert**
2. Alert type: **Threshold**
3. ClickHouse SQL query:
   ```sql
   SELECT count()
   FROM remote(t<source_id>_your_source_logs)
   WHERE JSONExtract(raw, 'summary.itemsFetched', 'Nullable(Int64)') = 0
     AND {{time}}
   ```
   Replace `<source_id>` with your source ID (visible in Betterstack → Sources → [source name] → Settings).
   > **Note:** If the query returns no results after live runs, the field path may differ. Try `raw LIKE '%itemsFetched%'` to confirm field is present, then adjust the JSONExtract path.
4. Threshold: ≥ **5** (five runs with 0 items fetched — D-24 revised threshold at 5-min cadence)
5. Confirmation period: **25 minutes** (5 runs × 5 min cadence)
6. Alert channel: email (PagerDuty/Slack deferred to v1.3)
7. Mute during burn-in (see §10)

### 9.2 Uptime Monitor (Heartbeat)

1. Log in to [https://uptime.betterstack.com](https://uptime.betterstack.com)
2. Go to **Monitors** → **New monitor** → Select **Heartbeat**
3. Configure:
   - **Name:** `reputation-oracle-sepolia`
   - **Expected heartbeat every:** `5` minutes (matches PKG-03 systemd timer cadence)
   - **Grace period:** `600` seconds (10 minutes = D-04; approximately 2 missed runs before alert)
4. Betterstack generates a heartbeat URL in the form: `https://uptime.betterstack.com/api/v1/heartbeat/<TOKEN>`
5. Copy the full URL
6. On the VPS: `sudo -u oracle nano /etc/reputation-oracle/sepolia.env`
   - Set `BETTERSTACK_HEARTBEAT_URL=<paste full URL here>`
   - Uncomment the line (remove leading `#`)
7. Restart the timer: `sudo systemctl restart reputation-oracle@sepolia.timer`
8. After the next run, verify in Betterstack Uptime that the monitor shows **Up** and the last heartbeat timestamp matches the run time.

**Alert channel:** Configure email alerts for the heartbeat monitor in Betterstack → Monitor → Edit → Escalation.

**Mute during burn-in:** In Betterstack Uptime, use the **Maintenance window** feature to suppress alerts during the 7-day burn-in period (§10). Remove the maintenance window after burn-in completes.

---

## 10. Burn-in Gate Procedure

**Purpose:** Validate that Phases 4+5+6+7+8 operate correctly end-to-end in production conditions before enabling the Mainnet timer (Phase 9). The gate is manual — an operator reviews the Betterstack dashboard and signs off.

**Duration:** 7 calendar days from the first successful heartbeat.

### 10.1 Gate Criteria

All of the following must be TRUE before enabling the Mainnet timer:

| # | Criterion | How to verify |
|---|-----------|---------------|
| B-01 | 7+ consecutive successful heartbeats (no `/fail` pings) | Betterstack Uptime → Monitor → History: 7+ green rows in a row |
| B-02 | Every log entry in Betterstack Telemetry has `runId` and `chainId` fields | Betterstack Logs → search `runId:*` — all runs should match |
| B-03 | No `systemicFailure` in any RunSummary during the burn-in period | Betterstack Logs → `systemicFailure:*` — should return empty |
| B-04 | `itemsFetched > 0` on all non-empty runs (subgraph reachable) | Betterstack Logs → `summary.itemsFetched:0` — zero matching entries outside intentionally empty runs |
| B-05 | No Betterstack Telemetry alert fired during the burn-in period | Betterstack Alerts → History — zero alerts |

### 10.2 Gate Sign-off

When all 5 criteria are met, document in the project state:

```
Phase 8 Sepolia burn-in complete.
Date: <YYYY-MM-DD>
First heartbeat: <runId of first successful run>
7-day window: <start date> → <end date>
B-01: ✓ (N consecutive clean heartbeats)
B-02: ✓
B-03: ✓
B-04: ✓
B-05: ✓
Gate OPEN — Phase 9 Mainnet Cutover may proceed.
```

Paste this into `.planning/STATE.md` under **Decisions** or create a dedicated `.planning/phases/08-observability/08-BURN-IN.md` file.

### 10.3 If Gate Fails

If any criterion fails during the 7-day window:

1. Identify the failure from Betterstack logs (filter by `runId` of the failed run)
2. Fix the root cause in the code
3. Deploy the fix via `sudo /opt/reputation-oracle/deploy/update.sh sepolia`
4. **Restart the 7-day window** from the first clean heartbeat after the fix
5. Document the failure and fix in `.planning/STATE.md`

The Mainnet timer MUST NOT be enabled until 7 consecutive clean heartbeats are observed after the most recent fix.
```

Verify the RUNBOOK.md looks correct by checking section headers:
```bash
grep -n "^## " deploy/RUNBOOK.md
```
Expected output should include §1 through §10.
  </action>

  <verify>
    <automated>grep -n "^## " /Users/jaybuidl/project/kleros/reputation-oracle/deploy/RUNBOOK.md && grep -n "BETTERSTACK_HEARTBEAT_URL\|Burn-in\|itemsFetched" /Users/jaybuidl/project/kleros/reputation-oracle/deploy/RUNBOOK.md | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep "BETTERSTACK_HEARTBEAT_TOKEN" deploy/RUNBOOK.md` — returns EMPTY (typo fixed in §2)
    - `grep "## 9. Betterstack Setup" deploy/RUNBOOK.md` — present
    - `grep "## 10. Burn-in Gate Procedure" deploy/RUNBOOK.md` — present
    - `grep "600" deploy/RUNBOOK.md` — grace period 600 seconds documented in §9.2
    - `grep "itemsFetched" deploy/RUNBOOK.md` — ClickHouse SQL query present in §9.1
    - `grep "B-01\|B-02\|B-03\|B-04\|B-05" deploy/RUNBOOK.md` — 5 gate criteria present in §10.1
    - `grep "7 calendar days\|7-day" deploy/RUNBOOK.md` — burn-in duration documented
    - Table of Contents updated: `grep "Betterstack Setup\|Burn-in Gate" deploy/RUNBOOK.md | head -5` — items present in TOC and section headers
  </acceptance_criteria>

  <done>RUNBOOK.md §2 typo fixed; §9 Betterstack Setup (Telemetry + Uptime steps + ClickHouse SQL alert) appended; §10 Burn-in Gate Procedure (5 criteria, sign-off format, failure handling) appended; TOC updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → bootstrap.sh heredoc | Placeholder values are commented out; no live secrets in bootstrap.sh |
| RUNBOOK.md → operator | Documentation-only; no code execution; no credentials stored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Information Disclosure | bootstrap.sh heredoc | accept | All Phase 8 placeholder lines are commented out (`# KEY=`); no actual token values stored in bootstrap.sh; filled by operator via nano (never echo) per §2 warning |
| T-08-16 | Availability | burn-in gate skipped | accept | Gate is manual and documented; no technical enforcement — organizational control; documented consequence: "Mainnet timer MUST NOT be enabled until 7 consecutive clean heartbeats" |
| T-08-17 | Information Disclosure | RUNBOOK.md ClickHouse source_id | accept | Source ID is a Betterstack-internal identifier visible only to account holders; RUNBOOK instructs operator to look it up in their own dashboard; not a secret |
</threat_model>

<verification>
After plan completion:
- `grep "BETTERSTACK_HEARTBEAT_TOKEN" deploy/bootstrap.sh deploy/RUNBOOK.md` — empty (typo gone)
- `grep "BETTERSTACK_HEARTBEAT_URL" deploy/bootstrap.sh` — present in heredoc
- `grep "HEARTBEAT_TIMEOUT_MS" deploy/bootstrap.sh` — present in heredoc
- `grep "## 9\|## 10" deploy/RUNBOOK.md` — both sections present
- `bash -n deploy/bootstrap.sh` — exits 0 (syntax valid)
</verification>

<success_criteria>
- bootstrap.sh heredoc: BETTERSTACK_HEARTBEAT_URL corrected, HEARTBEAT_TIMEOUT_MS=10000 added
- RUNBOOK.md §9: Telemetry setup + ClickHouse SQL alert rule for itemsFetched=0 × 5 runs
- RUNBOOK.md §10: 7-day burn-in gate with 5 criteria, sign-off format, failure/reset procedure
- §2 typo fixed (BETTERSTACK_HEARTBEAT_TOKEN → BETTERSTACK_HEARTBEAT_URL)
- Table of Contents updated
- bootstrap.sh bash syntax valid
</success_criteria>

<output>
After completion, create `.planning/phases/08-observability/08-06-SUMMARY.md`
</output>
