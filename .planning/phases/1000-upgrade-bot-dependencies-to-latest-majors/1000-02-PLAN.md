---
phase: 1000-upgrade-bot-dependencies-to-latest-majors
plan: 02
type: execute
wave: 2
depends_on:
  - "1000-01"
files_modified:
  - bot/package.json
  - bot/package-lock.json
  - bot/biome.json
autonomous: true
requirements:
  - UPG-03

must_haves:
  truths:
    - "Biome is upgraded to v2.x and lint/format checks pass"
    - "biome.json config uses v2 schema and assist.actions structure"
    - "All source and test files pass the new v2 recommended rules"
  artifacts:
    - path: "bot/biome.json"
      provides: "Biome v2 configuration"
      contains: "biomejs.dev/schemas/2"
    - path: "bot/package.json"
      provides: "Updated Biome devDependency"
      contains: "@biomejs/biome"
  key_links:
    - from: "bot/biome.json"
      to: "bot/src/**/*.ts"
      via: "biome check ."
      pattern: "assist.*organizeImports"
---

<objective>
Upgrade Biome.js v1 to v2 in the bot package, migrate config, fix any new lint violations.

Purpose: Biome v2 adds 50+ new recommended lint rules and restructures config. The automated migration tool handles config changes. New lint violations (if any) need manual fixes.
Output: Updated biome.json with v2 schema, all source files passing `biome check .`.
</objective>

<execution_context>
@/Users/jaybuidl/project/kleros/reputation-oracle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaybuidl/project/kleros/reputation-oracle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1000-upgrade-bot-dependencies-to-latest-majors/1000-RESEARCH.md
@.planning/phases/1000-upgrade-bot-dependencies-to-latest-majors/1000-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Biome to v2, migrate config, fix violations</name>
  <files>bot/package.json, bot/package-lock.json, bot/biome.json</files>
  <read_first>
    - bot/biome.json (current v1 config — will be migrated)
    - bot/package.json (current @biomejs/biome version)
  </read_first>
  <action>
    1. Install Biome v2: `cd bot && npm install -D @biomejs/biome@^2.4.9`
    2. Run automated config migration: `cd bot && npx @biomejs/biome migrate --write`
       This will:
       - Update $schema URL from `biomejs.dev/schemas/1.9.0/schema.json` to `biomejs.dev/schemas/2.x.x/schema.json`
       - Move `organizeImports.enabled: true` to `assist.actions.source.organizeImports: "on"`
       - Preserve formatter and linter settings as-is
    3. Verify the migrated biome.json matches expected structure:
       ```json
       {
         "$schema": "https://biomejs.dev/schemas/2.../schema.json",
         "assist": {
           "actions": {
             "source": {
               "organizeImports": "on"
             }
           }
         },
         "formatter": {
           "enabled": true,
           "indentStyle": "tab",
           "lineWidth": 120
         },
         "linter": {
           "enabled": true,
           "rules": {
             "recommended": true
           }
         }
       }
       ```
    4. Run `cd bot && npx biome check .` to find any new lint violations from v2 rules.
    5. If violations found: run `cd bot && npx biome check --write .` to auto-fix what Biome can. For remaining violations, fix manually following Biome's suggestions.
    6. Run `cd bot && npm run lint` to verify clean lint output.
    7. Run `cd bot && npm test` to verify tests still pass after any code reformatting.
  </action>
  <verify>
    <automated>cd bot && npm run lint && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - bot/package.json contains `"@biomejs/biome": "^2.4` in devDependencies
    - bot/biome.json contains `"$schema"` with `schemas/2` in the URL (not `schemas/1`)
    - bot/biome.json contains `"assist"` key with nested `"organizeImports": "on"`
    - bot/biome.json does NOT contain top-level `"organizeImports"` key
    - `cd bot && npm run lint` exits 0 (no lint/format violations)
    - `cd bot && npm test` exits 0 (all tests pass after any reformatting)
  </acceptance_criteria>
  <done>Biome v2 installed, config migrated from v1 structure to v2 assist.actions structure, all lint rules passing, all tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md stack table and verify final state</name>
  <files>CLAUDE.md, bot/package.json</files>
  <read_first>
    - CLAUDE.md (Technology Stack table — lines with zod, Biome.js, vitest version ranges)
    - bot/package.json (actual installed version ranges after all upgrades)
  </read_first>
  <action>
    Update the Technology Stack table in CLAUDE.md to reflect the new version ranges:
    - Change `zod` row from `^3.24` to `^4.3` with purpose unchanged ("Config validation with secret redaction")
    - Change `Biome.js` row from `^1.9` to `^2.4` with purpose unchanged ("Linting + formatting (replaces ESLint + Prettier)")
    - Change `vitest` row from `^3.1` to `^4.1` with purpose unchanged ("Bot unit/integration tests")
    - Also update the "Do NOT use" line if it references specific versions (it doesn't currently, but verify)

    Run full verification suite to confirm everything works together:
    ```bash
    cd bot && npm run typecheck && npm test && npm run lint
    ```
  </action>
  <verify>
    <automated>cd bot && npm run typecheck && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md Technology Stack table contains `^4.3` for zod (not `^3.24`)
    - CLAUDE.md Technology Stack table contains `^2.4` for Biome.js (not `^1.9`)
    - CLAUDE.md Technology Stack table contains `^4.1` for vitest (not `^3.1`)
    - All three verification commands exit 0
  </acceptance_criteria>
  <done>CLAUDE.md reflects actual installed versions, full test/lint/typecheck suite green</done>
</task>

</tasks>

<verification>
```bash
cd bot && npm run typecheck && npm test && npm run lint
```
All commands exit 0. biome.json uses v2 schema. CLAUDE.md version table matches package.json.
</verification>

<success_criteria>
- Biome upgraded from ^1.9 to ^2.4.9 with automated config migration
- biome.json uses v2 schema and assist.actions structure
- All source files pass v2 recommended lint rules
- CLAUDE.md Technology Stack table reflects new versions
- Full suite (typecheck + test + lint) green
</success_criteria>

<output>
After completion, create `.planning/phases/1000-upgrade-bot-dependencies-to-latest-majors/1000-02-SUMMARY.md`
</output>
