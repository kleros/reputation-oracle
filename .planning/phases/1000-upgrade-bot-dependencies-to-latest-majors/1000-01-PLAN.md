---
phase: 1000-upgrade-bot-dependencies-to-latest-majors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/package.json
  - bot/package-lock.json
autonomous: true
requirements:
  - UPG-01
  - UPG-02

must_haves:
  truths:
    - "zod is upgraded to v4.x and all existing tests pass"
    - "vitest is upgraded to v4.x and all existing tests pass"
    - "TypeScript compilation succeeds with no new errors"
  artifacts:
    - path: "bot/package.json"
      provides: "Updated dependency versions"
      contains: "zod"
    - path: "bot/package-lock.json"
      provides: "Locked dependency tree"
  key_links:
    - from: "bot/src/config.ts"
      to: "zod"
      via: "import { z } from 'zod'"
      pattern: "import.*zod"
    - from: "bot/test/*.test.ts"
      to: "vitest"
      via: "import { describe, expect, it, vi } from 'vitest'"
      pattern: "import.*vitest"
---

<objective>
Upgrade zod v3 to v4 and vitest v3 to v4 in the bot package.

Purpose: Align with latest major versions used across Kleros projects. Research confirms zero code changes needed for both — the bot uses only basic APIs that are backward-compatible.
Output: Updated package.json with zod ^4.3.6 and vitest ^4.1.2, all tests passing.
</objective>

<execution_context>
@/Users/jaybuidl/project/kleros/reputation-oracle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaybuidl/project/kleros/reputation-oracle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1000-upgrade-bot-dependencies-to-latest-majors/1000-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade zod to v4 and verify</name>
  <files>bot/package.json, bot/package-lock.json</files>
  <read_first>
    - bot/package.json (current dependency versions)
    - bot/src/config.ts (only file using zod — verify APIs used: z.object, z.string, z.coerce, z.infer, safeParse)
    - bot/test/config.test.ts (tests that must pass after upgrade)
  </read_first>
  <action>
    1. Run `cd bot && npm install zod@^4.3.6` to upgrade zod from v3 to v4.
    2. Run `npm run typecheck` to verify TypeScript compilation — zod v4 is a full rewrite but the APIs used in config.ts (z.object, z.string().regex(), z.coerce.number(), z.infer, safeParse, error.issues) are all backward-compatible.
    3. Run `npm test` to verify all 4 test files pass.
    4. If typecheck or tests fail: inspect the error. Per research, zero code changes expected. The only potential issue is if z.coerce.number() input type widened from `string` to `unknown` causes a TS error — fix by adding explicit type annotation if needed.
  </action>
  <verify>
    <automated>cd bot && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - bot/package.json contains `"zod": "^4.3` (v4 range)
    - `cd bot && npm run typecheck` exits 0
    - `cd bot && npm test` exits 0 with all tests passing
    - bot/src/config.ts still contains `import { z } from "zod"` (unchanged import)
  </acceptance_criteria>
  <done>zod v4 installed, typecheck passes, all tests pass, no code changes needed</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade vitest to v4 and verify</name>
  <files>bot/package.json, bot/package-lock.json</files>
  <read_first>
    - bot/package.json (current devDependencies)
    - bot/test/config.test.ts (test patterns used: describe, it, expect, vi.spyOn)
    - bot/test/diff.test.ts (test patterns used)
  </read_first>
  <action>
    1. Run `cd bot && npm install -D vitest@^4.1.2` to upgrade vitest from v3 to v4.
    2. Run `npm test` to verify all 4 test files still pass.
    3. Check test discovery: `cd bot && npx vitest run --reporter=verbose 2>&1 | head -40` — verify same test count as before (should be 4 test files).
    4. If test discovery changes (Pitfall 4 from research — v4 default excludes only node_modules and .git): create a minimal `bot/vitest.config.ts` with explicit exclude patterns. But this is unlikely given the bot has no dist/cypress folders.
    5. Vitest v4 requires Vite >= 6.0.0 (transitive dep, pulled automatically) and Node >= 20 (project uses Node 22 LTS — satisfied).
  </action>
  <verify>
    <automated>cd bot && npm test</automated>
  </verify>
  <acceptance_criteria>
    - bot/package.json contains `"vitest": "^4.1` (v4 range) in devDependencies
    - `cd bot && npm test` exits 0 with all tests passing
    - Same number of test files discovered (4 files: config.test.ts, diff.test.ts, evidence.test.ts, validation.test.ts)
  </acceptance_criteria>
  <done>vitest v4 installed, all 4 test files discovered and passing</done>
</task>

</tasks>

<verification>
```bash
cd bot && npm run typecheck && npm test
```
Both commands exit 0. No source files modified (only package.json and package-lock.json).
</verification>

<success_criteria>
- zod upgraded from ^3.24 to ^4.3.6, zero code changes
- vitest upgraded from ^3.1 to ^4.1.2, zero code changes
- All existing tests pass
- TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/1000-upgrade-bot-dependencies-to-latest-majors/1000-01-SUMMARY.md`
</output>
